#include<stdio.h>
#define NM
int main(void)
{
	int a[N], i;
	printf("ENTER %d:" N);
	for(i = 0; i <= N; i++)
	scanf("%d", &a[i]);
	printf("in reverse: ");
	for(i = N; i >=0; i--)
	printf("%d", a[i]);
	return 0;
}
